/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // 외부 이미지 도메인 설정(안하면 못씀)
  // images: {
  //   domains: ["cdn.pixabay.com"],
  // },
};

module.exports = nextConfig;
